{
  "name": "klyro-extension",
  "version": "1.0.0",
  "description": "Chrome extension to summarize LinkedIn posts using AI",
  "scripts": {
    "convert-icons": "node convert_icons_browser.js",
    "install-deps": "npm install",
    "setup": "npm install && npm run convert-icons",
    "test": "jest"
  },
  "dependencies": {
    "puppeteer": "^21.0.0"
  },
  "keywords": ["chrome-extension", "linkedin", "ai", "summarization"],
  "author": "Ciaran McAleer",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
